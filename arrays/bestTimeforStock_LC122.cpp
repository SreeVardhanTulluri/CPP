#include <iostream>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <unordered_map>

using namespace std;

class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int buyDay = 0, sellDay = 1, totalDays = prices.size(), profit = 0;
        
    }
};

int main(){
    vector<int> prices = {7,1,5,3,6,4};
    cout<<(new Solution)->maxProfit(prices)<<endl;
}